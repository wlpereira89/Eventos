@model IEnumerable<Modelo.DAO.resposta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.resposta1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pergunta.pergunta1)
        </th>
        <th>
            Evento
        </th>    
        <th>
            @Html.DisplayNameFor(model => model.usuario1.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.resposta1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pergunta.pergunta1)
        </td>
        <th>
            @Html.DisplayFor(model => item.pergunta.evento.Nome)
        </th>    
        <td>
            @Html.DisplayFor(modelItem => item.usuario1.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
