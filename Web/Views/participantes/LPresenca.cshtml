@model IEnumerable<Modelo.DAO.participante>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>LPresenca</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Presenca)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.evento.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.usuario.pass)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.evento.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.usuario.pass)
            </td>
            <td>
                @Html.EditorFor(modelItem => item.Presenca)
                @Html.ValidationMessageFor(modelItem => item.Presenca, "", new { @class = "text-danger" })
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
